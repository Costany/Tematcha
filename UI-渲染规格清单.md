# Tematcha · UI 渲染规格清单

> 版本 v0.2 ｜ 2026-09-17（四项设计定稿，见 §15 决策记录）｜ 依据：letcode v0.14.0 ACP 源码取证 + letcode 自带 TUI 截图观察（13 张，见 `refs/screenshots/`）
>
> 阅读方式：每块 = **数据来源 → 视觉形态 → 键位/交互**
> 标记：✅ 协议可得 ｜ ⚠️ v1 受限 ｜ 💤 愿望清单（需上游扩展）

## 0. 总体布局

```
┌──────────────────────────────────────────┬──────────────┐
│ 消息区（滚动）                              │ 右侧栏（可折叠）│
│  用户消息 / 助手回复(Markdown) / 思考块      │  会话信息      │
│  工具卡 / 权限回执 / 错误块 / 摘要条          │  上下文条      │
│                                          │  待办          │
├──────────────────────────────────────────┴──────────────┤
│ 工作区（M11）：忙 = 工作行 / 压缩进度条 · 闲 = 收尾行（1 行） │
├─────────────────────────────────────────────────────────┤
│ 输入区（❯ 前缀 + 上下规则线）                               │
├─────────────────────────────────────────────────────────┤
│ 状态栏：模式徽章 ｜ 上下文 miniBar % ｜ 模型 · 思考 ｜ 提示   │
└─────────────────────────────────────────────────────────┘
（权限面板 / 命令弹层在输入区上方弹出；默认不画底、终端透明优先）
```

消息区右缘贴一列滚动条（M4a，见 §10）：内容溢出时 `┃`（拇指，强调绿）+ `│`（轨道，深灰）；不溢出时留白；右栏可见时该列常驻，右栏不因条的出现/消失而横移。

消息区底部还有钉面板（M5b，见 §5；2026-09-19 起从顶部搬到底部，照 Claude Code）：`# Todos · 计数` + 待办列表，固定不随消息滚动；占几行、消息区就让几行（整屏行数守恒）。

输入区正上方还有工作区（M11，见 §11.6）：一行高——忙时是灵动工作行或压缩进度条（§8），闲时是上一回合的收尾行；占行时消息区相应让位（整屏行数守恒）。

## 0.5 视觉原则（2026-09-17 用户要求）

- **终端透明优先**：默认不输出背景色，让终端自身的透明/磨砂背景透过来；**禁止大面积底色**
- 层级表达用：前景色 / 字符符号 / 线条（边框、竖条），不用色块
- 小面积反白仅限光标类元素（≤2 字符）
- 主题的 `*_bg` 系 token 默认不落地（未来可选 bg-mode：none/subtle）

## 1. 消息区

### 1.1 用户消息

- 数据：`user_message_chunk`（提交 prompt 后 / 会话重放时）✅
- 形态：左侧竖条（**无底色**）+ 缩进正文（等宽）；不用气泡框、不铺背景（省宽度、透明优先）
- **回显去重（实装，2026-09-18）**：客户端本地回显 + 引擎在回合开始回发同文本 `user_message_chunk`（实机 trace 取证）——用 `localEcho` 登记待消费，回发同文本时吞掉（会话重放的 loading 期间照收，不吃历史消息）。修复前症状：同一条用户消息显示两次（用户截图实况）；自检 `-echotest` 5 项
- **现状（M1.7 实装）**：`│` 细线（强调绿 `#00E7A4`）+ 空格 + 正文，多行时**每行都带竖条**；**鼠标左键点击该条 → 竖条换成半块 `▌`（视觉加粗）**，点别处取消（两态照 crush 的 UserBlurred/UserFocused）。❯ 前缀在消息区退役（输入区的 ❯ 提示符保留）

### 1.2 助手回复（Markdown 渲染 · 重点）

- 数据：`agent_message_chunk`（流式，带 `message_id`；同 id 累积为一条消息）✅
- 形态：
  - 使用 **glamour v2**（charm.land/glamour/v2）渲染：标题 / 粗体 / 行内代码 / 列表 / 引用 / 链接
  - 代码块：圆角边框 + 左上语言 chip（`python`/`json`…）+ 语法高亮（**无底色**，仅边框与前景色）
  - 表格：粗表头 + 分隔线 + 列对齐（宽度按 lipgloss.Width 计算）
  - 流式：到达即追加；尾端可选 `▍` 光标；完成后光标消失
- 交互：天然打字机（无需动画）；流式期间自动滚底；用户上滚时暂停跟随
- 备注：glamour 默认不做色彩降采样——按官方方式用 lipgloss 输出；样式表换肤接主题系统
- **现状（M1.6 实装）**：回答区**不再带任何前缀符号**（铅笔 ✎ 退役）；整块 2 格缩进、无前缀，轮次区隔交给回合分隔行（§1.7）。glamour 透明样式 / OSC 8 剥离 / 行尾填充剥离见 `md.go`；粗体 = 白 + bold
- **现状（M1.7 实装）**：行内代码配色由 glamour dark 默认的粉红（256 色 203 = `#FF5F87`）改为强调绿 `#00E7A4`（`md.go` 的 `transparentDarkStyle` 里改 `Code.Color`）；表格/列表里的 `` `文件名` `` 等因此跟着变绿
- **现状（M1.8 实装，2026-09-18）**：回答按"段落"渲染（与思考同款边界：其它类型事件到来 = 段落结束）——每次 LLM 迭代的正文（工具调用前的过渡语、最终总结）各自成为独立条目、出现在流的当时位置。教训：早先一个回合共用一个条目槽位，多段正文全并进第一段出现的位置（画面外上方），最终回复看起来像"没回就结束"（详见 §15 ⑮；同日加固：迟到 chunk 并回上一段，防"应答先到"的通道竞争把正文尾巴挤到分隔行下方）

### 1.3 思考块（Thought）

- 数据：`agent_thought_chunk` ✅（**无 message_id** → 按"回合内时间段"分块）
- 形态（**定稿·混合**）：流式时**实时显示全文**（呼吸省略号）→ 思考结束**自动折叠为一行** `Thought · 1.2s`（暗色）→ enter 可再展开；无思考的模型整块隐藏
- 交互：enter 展开/收起（可覆盖自动行为）；时长=首/末 chunk 时间差
- **现状（M1.6 实装 · 对齐 /thoughts 的 Full 档）**：思考按"周期"分块（ACP 无周期边界，以"其它类型事件到来"为界）——每块渲染 `~ <标题>` + **全文**（正文 4 格缩进、暗色）+ **块尾耗时行** `· 2s`（暗色小字，贴着块尾）。标题 = 思考文本第一行非空行（清洗 `#`/`**`/`` ` ``；`__` 仅整行包裹时剥，避免打断 `fs__read` 这类标识符）；耗时进行中实时增长、周期结束定格（格式 `123ms / 12s / 2m 30s`）。耗时位置定稿 = **块尾独立一行**（曾试过放标题前，被否）：标题常是一整句中文，跟在后缀会被折行埋掉、流式时还来回跳；放块尾位置稳定，也不与标题抢注意力

### 1.4 工具卡（核心组件）

- 数据：`tool_call` + `tool_call_update` ✅（toolCallId、title、kind(read/edit/search/execute/fetch/think/other)、status、rawInput、content 整体替换）
- 三种状态：
  - 运行中：`⌛ Run <title>`（或 `→`）+ 呼吸点；输出 tail-follow 末 N 行
  - 完成：折叠为一行 `→ <title>` + 可选展开
  - 失败：红后缀 `· failed` + 输出块
- 卡片解剖：`[图标] 动作+目标` ｜ 状态符 ｜ 展开区=格式化 rawInput + 输出（等宽）
- 聚合：连续同类折叠 `read 2 · checks 1 · expand`（抄 letcode 语法）
  - **edit 特例**：红绿双栏 diff + 行号（**前景色表达：旧行红系 / 新行绿系 + 行首 `-`/`+` 标记，不铺底色**）+ `← Patch <path>` + `Patch N edits`（基于 content 文本渲染）
- 展开策略（**定稿**）：运行中**自动展开** + tail-follow 末 N 行；完成后**自动折成一行**；手动 enter 覆盖自动
- 交互（**定稿**）：鼠标点击展开/收起 = **v1 已实装**（M2b，复用命中测试）；键盘选择态 = **v2 已实装**（M2b v2：Tab 进出、↑↓ 选卡、enter 展开收起、esc 只退模式）；滚轮照旧滚动消息区；客户端计时 `0.8s` 小字
- **现状（M1.6 实装 · 一行式）**：`⌬ <调用摘要> · <结果摘要>`；状态由**齿轮颜色**表达——进行中灰 `#9A9A9A`、成功柔绿 `#79C77D`（不再输出"完成"二字）、失败柔红 `#C97B7B`。引擎三阶段 title（pending=工具名 / in_progress=调用摘要 / 完成=结果摘要）**分字段留存**，完成时仍能看到"是什么工具"（否则完成更新会把工具身份覆盖成只剩结果）；结果摘要放不下时另起一行（4 格缩进、暗色）
- **现状（M2b 实装，2026-09-18 · 卡片化 v1）**：鼠标点击整卡展开/收起 + 自动三态（运行中自动展开、完成折叠、**失败默认展开**——stderr 就在尾部几行；用户点过之后不再自动切换）。展开区 = 输入（shell 取 `command` 呈 `$ …` 单行；其余给紧凑 JSON 暗色）+ 输出尾部（末 8 行、行首暗灰 `│` 竖条；被截时补 `… 共 N 行 · 显示末 8 行`）。展开指示 `▸/▾` 跟在齿轮后（无内容不显示）。聚合折叠（`read 2 · checks 1`）与 edit diff 特例：**M6 已实装**（见下条）
- **现状（M2b v2 实装，2026-09-18 · 键盘选择态）**：`Tab` 进入——`↑↓` 在"有内容的卡"之间移动（无内容卡跳过；进入时默认落在窗口里可见的最后一张，移动时自动滚动保证头行可见）；`enter` 展开/收起（置 ToolUserSet，同鼠标点击）；`esc`/`Tab` 退出（**esc 只退模式，不取消回合**）；其余按键退出模式后照常处理（打字直接续上）。选中卡头行两格缩进换成 `▌`，**颜色跟随该卡状态**（与齿轮同色：成功柔绿 `#79C77D` / 失败柔红 `#C97B7B` / 进行中灰 `#9A9A9A`；用户 2026-09-18 修订）——一眼看出选中的卡是成是败；宽度不变、无底色。状态栏左侧「选择工具卡 i/n」、右侧换成选择态键位提示。自检：`-navtest`（状态机断言）与 feedtest 两态配色样张
- **现状（M6 实装，2026-09-18 · 聚合折叠 + diff 特例）**：连续 ≥2 张“完成且折叠”的工具卡（非失败/非选中/非子代理）合并为一行 `⌬ ▸ read 3 · checks 1 · expand`（family：read/edit/checks/run/fetch/tool）；点击聚合行展开组内首张有正文的卡（其余继续聚合、逐张吐出）；失败/进行中卡天然打断；消息条选择态期间禁用聚合（逐卡导航）。`edit__apply_patch` 展开首段 = 红绿 diff：`← Patch <path>` + 旧行红 `-` / 新行绿 `+`（前景色 + 行首标记、不铺底色）；宽 ≥50 双栏（左旧右新、`│`、预览行号）、窄屏单栏；输出与摘要同文时跳过。自检：`-feedtest` 扩展（三连折叠/失败打断/选择态禁用 + diff 双栏单栏红绿宽度）
- **失败判定（2026-09-18 修订 · 命令级失败）**：引擎只在"工具执行出错"时给 failed——**shell 的非 0 退出码不算失败**（exit code 是结果数据；证据：letcode `acp/projection.rs` 的 Failed ⟺ ToolOutcome::Failure、`session/formatting.rs` 的 summarize_command 把 `exit N` 当摘要、`runner.rs` 的 ok 标志只看工具错误）。前端补一层推导：completed 摘要以 `exit <N>` 开头且 N≠0 → 视同失败（`ToolBad`）——红齿轮 / 红选中条 / 默认展开；判据覆盖 `shell__exec` 与 `git_status/diff/log` 这些同格式工具。注：原版 letcode 不做这层推导（非 0 退出按成功呈现），这是我们多补的一刀（用户要求）

### 1.5 错误块

- 数据：引擎 error 事件 / ACP 错误响应 ✅（例："a prompt turn is already running"、"letcode could not compact the session context: …"、"Fast mode unavailable"、"Usage: /model <id>"）
- 形态：`error` 小标签 + 红字正文 + 浅红左条；保留在历史可回看
- **宽度预算（M4e 修，2026-09-18）**：错误行前缀 = 「2 格缩进 + `┃ ` 2 格 + 标签 + 1 空格」共 9 格，`textW = 总宽 - 前缀实测宽`；**曾经按 gutter 1 格算** → 整行超预算 2 格，右缘的滚动条列与右栏被"顶着"往右挪（用户原话"右侧顶出去了"；像素取证：错误行的滚动条列比邻行右移 24px ≈ 2 格）。两道保险：`-feedtest` 逐行宽度断言 + `View()` 组装时 `clipLine` 兜底截断（ANSI 感知，超宽才触发）
- **"怎么办"提示行（M4e 新增）**：`FeedItem.Detail` → 错误块尾补一行暗色 `提示: …`（命令用法类 → 引导用 `/` 补全；本地专有命令 → 说明 ACP 下不可用；推理档位被拒 → 提示换模型 / 查可选值）
- **折行规则（M4e 打磨，2026-09-18）**：`wrapText` 的折行点优先落在空白（词边界）上——英文 / 路径 / 命令名不被拦腰截断（此前会出现「…或用 /reasonin」+「g 查看可选值」）；找不到空白（超长 URL、长代码行）才退化成按宽度硬切；折行点上的空白被吃掉，不飘到下一行行首。`-feedtest` 有对应断言（样张 `["提示:" "当前模型不接受该推理档位——可先" "/model 换模型，或用" "/reasoning 查看可选值"]`）

### 1.6 摘要/回执条（绿条）

- 用途：批准回执 `▌ approved shell__exec … · Allow once`、说明 `▌ 说明：…`、改动 `▌ 改动：…`
- 数据：本地生成（批准记录来自我们对权限请求的应答）✅
- 形态：绿竖条 `▌` + 灰白文字（letcode 同款）
- **现状（M2a 实装）**：回执 = 新增条目类 `kReceipt`（`▌` 用强调绿 `#00E7A4` + 灰白文字，前景色 only）；审批占位从 `→ 等待批准…` 原地换成回执文案 `已批准 · <title> · <option name>`（取消/失效则换成暗色说明）

### 1.7 回合分隔行（M1.6 新增）

- 形态：`◇ <模型名> via <供应商> in <总耗时>` + 右侧补满横线（观感照 crush；例：`◇ Step 3.7 Flash via guji in 12s ────────────`）
- 数据：模型/供应商来自 `session/new` 的 configOptions（model 选项：`value` = "供应商/模型" 路由名、`name` = 显示名；如 `guji/step-3.7-flash` / `Step 3.7 Flash`），`config_option_update` 通知保持同步；耗时 = 用户发送时刻 → 回合结束（本地计时，取消 / 出错分支同样落行）
- 目的：给"一轮用户 + AI 完整对话"封口——回答区已无前缀符号（§1.2），轮次区隔靠它
- 颜色：**整行无彩色**（菱形与文字同为暗灰 `#6E6E6E`，横线 `#464646`）；窄屏放不下时退化成普通折行、不画横线

### 1.8 消息复制（M6 新增，2026-09-18）

- 触发：鼠标右键点击任意消息条目（消息区命中测试，口径同左键；右栏区域忽略）；工具卡选择态 `ctrl+y` 复制选中卡；非选择态 `ctrl+y` 复制最后一条助手回复
- 通道：`tea.SetClipboard`（OSC 52，终端转发）+ Windows 本地 PowerShell `Set-Clipboard` 兜底（UTF-8 base64 → `-EncodedCommand`，两路同发、谁生效都行）
- 文本口径：用户/回复/思考 = 原文；工具卡 = 调用摘要 + `$ 命令`（或参数）+ 输出；错误 = 错误文本 + 「提示」行
- 回执：绿条「已复制…（N 字）」；空内容不响应；无回复 → 琥珀提示
- 自检：`-copytest`（文本四类 + 空条目 + PS 命令编码可还原 + 回执两态）

## 2. 权限流（非 YOLO · 核心）

- 数据：`session/request_permission`（引擎 → 前端请求）：`toolCall{title, kind, rawInput}` + `options[{optionId, name, kind}]` ✅
- 形态（**定稿·混合式**）：底部面板负责操作（输入区上方、随内容上推、不悬浮遮挡、无底色）；消息区同步插一条 `→ 等待批准…` 占位，答完面板收起、占位换成 §1.6 绿条回执：

```
⚠ <title> needs approval
<命令/参数正文（pre 格式）>
模式
{ rawInput 格式化 JSON }（可折叠）
[ 允许一次 ]   [ <动态文案：如 "Allow cargo test always"> ]   [ 拒绝 ]
y 允许一次    a 始终允许    n 拒绝    esc 取消
```

- 交互：y/a/n/esc；**always 的文案必须用引擎给的 name**（grant_summary 动态生成）
- 队列：并发请求排队（"还有 N 个待处理"），一次答一个
- esc：先 `session/cancel`（若在回合中）再应答 `cancelled`（引擎侧按 deny 处理，兜底不挂起）
- **现状（M2a 实装，2026-09-17）**：
  - 面板钉在输入区上方：占几行、消息区就矮几行（`syncLayout` 统一算高度），左侧琥珀竖条 `┃` + 文字（透明度原则，无底色）。内容 = `需要批准 · <title>` → `$ <rawInput.command>` → rawInput 紧凑 JSON（最多 3 行、暗色）→ 按键提示（按 options 实际给的 kind 拼，always 用引擎 name）→ `还有 N 个待处理`
  - 键位：`y`=allow_once、`a`=allow_always、`n`=reject_once、`esc`=取消（先 session/cancel 再逐条回 cancelled）；**面板期间输入框不吃键**（模态防误输入），滚动/滚轮照旧
  - 消息区联动：进队即插 `→ 等待批准… <title>` 占位；答完占位变绿条回执 `▌ 已批准 · <title> · <option name>`；取消/失效（回合结束、引擎断开）换成暗色说明
  - **协议要点（踩坑实录）**：响应体是双重嵌套 `{"outcome":{"outcome":"selected","optionId":"…"}}`（取消：`{"outcome":{"outcome":"cancelled"}}`）；更关键的是——**反向请求的 JSON-RPC id 是字符串 UUID，必须原样回传**。早期实现把 id 一律转成数字回传，引擎的 pending 表对不上，表现为"回合永久卡死、进程 CPU 为 0"。现由 `ACPEvent.RawID any` 全程透传（见 acp_client.go）
  - **always 的展示（2026-09-17 修订）**：引擎给的 always 文案是**记住范围**（实测形如 `shell__exec: Remove-Item demo-lab/hello.txt`），不是一句人话。按键行只放短标签 `a 始终允许`；范围另起一行、暗色、超长截断 → `始终允许范围：<引擎文案>`。早先直接把 name 塞进按键行，看起来像又一条命令（用户反馈）
  - **回执动词随选项性质**：allow*\* → `已批准`、reject*\* → `已拒绝`（早先写死"已批准"，按 n 拒绝也显示已批准——已修）；回执 detail 截断到 48 格
  - **端到端实测（无 TTY）**：`-smokeallow`（允许）→ 工具 in_progress → completed；`-smokedeny`（拒绝）→ `user denied permission (failed)` → 回合正常收尾。两条链路都以真实引擎跑通
  - **M5 美化参考**：crush 的 Permission Required 弹窗（居中卡片、边框无底色、`Tool/Path/Desc` 字段行、命令独立块、`Allow · Allow for Session · Deny` 选项行 + `←/→ choose · enter confirm · esc exit` 提示行）。当前保持"输入区上方面板"形态（不遮挡、无底色）；M5 统一主题时再评估是否改为居中卡片
- 应答格式实锤：`{"outcome":{"outcome":"selected","optionId":"allow_once"}}`（id 原样回显）
- 来源徽章：子代理发起的请求标题带 `agent_name` 前缀 → 渲染 `explorer ›` 徽章

## 2.5 追问表单（Elicitation · question 工具 · M5a）

- 协议（ACP schema 1.7.0 `v1/elicitation.rs` + letcode `src/acp/driver.rs` 取证）：
  - 能力广告：`initialize` 的 `clientCapabilities.elicitation.form = {}` —— 不广告的话引擎把 question 工具直接判为 `the ACP client does not support form elicitation` 并自动婉拒
  - 引擎 → 客户端（反向请求 `elicitation/create`）：`{sessionId, mode:"form", message, requestedSchema:{type:"object", properties:{"question_N":{type,title,description,oneOf:[{const,title,description}]}}, required:[…]}}`；`type=array` = 多选（选项在 `items.oneOf`）
  - 客户端 → 引擎：`{"action":"accept","content":{"question_1":"<标签>","question_2":["<标签>",…]}}`（单选=字符串、多选=字符串数组；未作答的题不进 content）；`{"action":"decline"}`（用户不想答）；`{"action":"cancel"}`（客户端取消/不支持的 mode）
- 面板形态（钉在输入区上方，与权限面板同一套"钉子位"；左缘**强调绿**竖条、无底色 —— 与权限面板的琥珀区分语义：绿=交流提问，琥珀=需要批准；**M7 重定调，2026-09-19**）：

```
  ┃ 第 1/2 题 · <message 首行>        ← 强调绿加粗标题；单题时只写「第 1 题」
  ┃ ▸ 1/2 <题面标题>
  ┃     <题面说明（折行）>
  ┃     ❯ 1) <选项标题>  <选项说明（暗色）>
  ┃       2) <选项标题>
  ┃       0 自己写…                  ← 自定义答案入口（M7）
  ┃   2/2 <题面标题>     ← 非当前题只留题头（已答时补一行 ✓ 已答：<选项>）
  ┃ （空一行）
  ┃ space 选/勾 · 0 自己写 · ↑↓ 移动 · ←→ 换题 · enter 提交 · esc 拒绝   ← 整行暗色，与选项拉开
```

- 键位：`↑↓` 选项移动 / `←→`·`tab`·`shift+tab` 换题 / `space` 勾选（多选切换、单选选中后自动跳下一道未答题）/ `1..9` 直选 / `0` **自己写**（M7 新增：进入自定义答案输入态，可打印字符入缓冲、backspace 删除、enter 确认后跳题或提交；单选进入时会清掉已选项避免双答案；esc 在输入态先退出输入态、再按才是拒绝）/ `enter` 全答完则提交、否则跳下一道未答题 / `esc` 拒绝（回 decline）
- **自己写（M7 · 引擎依据）**：letcode `question.rs` 里 `options` 虽为必填，但 `validate_question_response` 只校验答案**非空且数量匹配**（单选 ≤1），不限制必须来自选项 —— 自定义文本是引擎原生接受的答案；accept content 里原样回传文本（多选题 = 已勾选项 + 自写文本一起进数组）
- 占位与回执：消息区先落暗色 `→ 等待回答… <message 首行>`；提交后原地换绿条 `已回答 · N 题 · 题1=…`，拒绝换 `已拒绝回答 · …`
- 边界：不认识的 mode（url）直接回 `cancel`（不渲染面板）；回合结束/引擎断开时丢弃未答表单（同权限面板）
- 自检：`-elicittest`（8 项：解析含自由输入题/url 不支持/渲染含边条强调绿与自写行/键位含 0 自写中文与提交收桌/accept 体形状含自写=原文/decline + 未答不进 content/队列/View 集成行数守恒）；真机探针：`-smokeelicit -smoke "…用 question 工具问我…"` → 实测引擎发 `elicitation/create`、自动作答后 `question 2 fields (completed)`

## 3. 状态栏

- 数据：`usage_update(used, size)`、`current_mode_update`、`config_option_update(model/reasoning)`、session title ✅
- 形态（左 → 右）：模式徽章（safe/default/auto/yolo 着色）｜ 上下文 miniBar + `%` + `31.8k/320k` ｜ 模型名 · 思考档 ｜ `/help`
- 瞬态位：`等待批准` / `引擎连接断开`（覆盖显示；压缩瞬态 M11 起归工作区，见 §8）
- ⚠️ 拿不到：token ↑↓、t/s 速率、分段明细（t/s 可用 chunk 计数客户端粗算，可选）
- 阈值色：<60% 绿 / <85% 黄 / ≥85% 红
- **现状（M3 实装，2026-09-18）**：左 = `letcode · 引擎状态 · 模式徽章 · 上下文条 · 模型名[· 思考档]`，右 = 快捷键提示（跟随忙闲/权限/选择态）。模式徽章配色：safe 柔绿 `#8CE28A` / default 中性 `#D0D0D0` / auto 琥珀 `#F5A25C` / yolo 柔红 `#C97B7B`；上下文条 10 格（`█`+`░`）+ `%` + `87.3k/320k`（fmtK 紧凑写法），阈值色 <60 绿 / <85 黄 / ≥85 红。数据：session/new 的 `modes.currentModeId` + configOptions 的 `mode`/`reasoning_effort`，`current_mode_update`、`config_option_update`、`usage_update` 增量同步。窄屏从尾部丢段：模型 → 上下文条 → 模式徽章（至少保留引擎状态）；模态（权限/工具卡选择态）抢占左侧。自检：`-statustest`（7 个样张 × 宽度断言 + 阈值色 SGR 核对 + 背景色检查全绿）。**M11 起**：动态内容（工作行 / 压缩进度 / 收尾行）全部搬去输入栏正上方的工作区（§11.6）——左段只留轻量静态词（空闲 / 载入会话 / 引擎已退出），忙时为空（不两处重复）；降级链不变：尾部先丢模型 → 上下文条 → 模式徽章（排队段比它们留得久）

- **M3c 输入队列（2026-09-18 实装）**：回合进行中提交的消息不再被拒——排队后自动接着发。状态栏在引擎状态右边加一段「排队 N」（琥珀；降级从尾部丢段，它比模式/上下文/模型留得久）

## 4. 右侧栏（可折叠）

- 会话信息：标题（session_info_update）✅ ｜ 模型 ✅ ｜ 权限模式 ✅ ｜ 会话 id ✅（分支/节点 ⚠️）
- 上下文：总条 + used/size + 剩余容量 ✅（分段明细 💤）
- 待办：小列表 + `待办 3/7` 计数 ✅（详见 §5）
- 交互（**定稿·ctrl+b**）：ctrl+b 开关 + `/panel on/off` 命令兜底 + 窄屏自动隐藏（宽度 <100 列折起，宽了恢复）
- **现状（M3b 实装，2026-09-18）**：消息区右侧「 │ 」分隔列 + 28 格信息栏（会话标题/模型/模式/会话 id 短码 → 上下文 used/size/剩余 → 待办 `○/◐/✓` 列表 + `待办 1/4` 计数；超长截断、放不下时 `… 还有 N 条`）。`ctrl+b` 开关（默认开）；`<100` 列自动隐藏、宽了恢复；鼠标点击右栏区域不落到消息区。数据：`session_info_update.title`、`plan.entries`（全量快照；blocked→pending、cancelled→completed 有损映射接受）。自检：`-paneltest`（样张 + panelVisible 判定 + parsePlan + View() 集成抽查全绿）

## 5. 待办（Plan）

- 数据：`plan` 通知 —— **全量快照**（每次整体替换；content/status；无 id；优先级恒 medium）✅
- 形态：
  - 右栏小列表：`○` 待办 / `◐` 进行中 / `✓` 完成（完成=暗色）
  - 消息区一次性钉面板（`# Todos`，letcode 同款），新快照时更新，`/todos` 可收起
- 注意：blocked→pending、cancelled→completed 有损映射（接受，视觉无法区分）
- **现状（M3b 实装，2026-09-18）**：右栏小列表已实装（`○/◐/✓` + `待办 1/4` 计数）
- **现状（M5b 实装，2026-09-18；2026-09-19 搬家）**：消息区底部钉面板已实装 —— `# Todos · 1/4` 标题 + `○/◐/✓` 列表（完成项暗色）；固定不动、不参与滚动；最多 6 条、超出折成 `… 还有 N 条`；`/todos` 开关（客户端命令：不发引擎、不进历史、不新开回合）；数据随 `plan` 全量快照刷新（行数变化 → 消息区高度自动重算）；宽度按显示宽裁剪、只前景色。自检：`-todostest`（渲染/超长截断/开关/布局让位/View 集成 5 项全绿）

## 6. 输入区

- 形态：上下规则线 + `❯` 前缀；rune 级编辑（CJK 安全）+ 水平窗口滚动 ✅
- 回合锁：进行中禁发（引擎拒并发），提示 `esc 取消`；esc → `session/cancel`
- 粘贴/中文/IME：统一走 `msg.Text` 路径
- 历史：↑/↓ 翻历史输入（M4+ 可选）
- **现状（M4c 实装，2026-09-18）**：↑↓ 的归属分两档 —— 输入框**空着**（或正在翻历史）时 ↑↓ 翻历史；**有草稿**时 ↑↓ 照旧滚消息区（滚轮 / PgUp / PgDn 全程不受影响）。历史只活在内存（不落盘、跨会话不保留）；空串与连续重复不入栈；↓↓ 越过最新一条时还原"开翻前的草稿"并退出浏览；翻出来后**手改一个字即退出浏览**（此后 ↑↓ 回滚动）。自检：`-histtest`（12 项）

- **现状（M3c 实装，2026-09-18）· 回合锁改为队列**：回合进行中提交 → 消息入队并在消息区落一条 `kQueued`（形态同用户消息，但整体暗色：暗灰竖条 + 暗字 + 尾行「· 排队中」）；当前回合结束（正常 / 取消 / 出错都算）后队首**原地转正**（Kind 换回 kUser → 绿条亮字，位置不动、不产生第二条消息）并自动开下一回合。FIFO；排队时同样进输入历史（↑↓ 可召回）；状态栏显示「排队 N」。`/resume` 在回合进行中仍走提示（载入会重放历史，要独占一个空闲回合）；命令护栏优先于排队（必拒命令不会被排进队列）。自检：`-queuetest`（6 项）

## 7. 命令弹层（`/`）

- 数据：`available_commands_update`（permission/model/reasoning/compact/fast/new/resume/undo/redo）✅
- 形态：输入 `/` 弹出、过滤、最多 8 行、选中 `❯` 高亮（agent-ui 同款）
- 交互：↑↓ / tab 补全（补 `name `）/ enter 执行 / esc 关闭；执行=作为 prompt 文本发送
- 错误提示：直接展示引擎文案（如 "Usage: /permission <safe|default|auto|yolo>"）
- **现状（M4b 实装，2026-09-18）**：新文件 `ui_cmd.go`（parseCommands / palOpen / palMatches / palSelAt / renderCmdPalette / palKey / completeCmd）
  - 数据形状（实机 trace 核对）：`{"sessionUpdate":"available_commands_update","availableCommands":[{"name":"permission","description":"…","input":{"hint":"safe|default|auto|yolo"}}, {"name":"compact",…}]}` —— 无参数的命令没有 `input`；会话建立后下发
  - 打开条件：输入以 `/` 开头且**还没打空格**（一旦写参数就收起，enter 照常发送）；esc 只关弹层、输入保留（再打字即恢复）
  - 键位定稿：↑↓ 选择（夹取不绕圈）/ tab 补全 / enter —— **无参数命令直接执行，带参数命令只补全到 `/name `**（参数留给用户，防误发）；弹层开着时 ↑↓/tab 不再归滚动与工具卡选择态（模态优先）
  - 行数与降级：最多 8 行（多出显示「… 还有 N 条」）；名字列（含提示）超 3/4 宽 → 丢参数提示；描述列 < 12 格 → 丢描述
  - 布局：钉在输入区上方，行数在 syncLayout 里从消息区扣出（整屏行数恒定，与权限面板同一套机制）
  - 自检：`-cmdtest` 22 项（解析 / 命令词 / 过滤 / 两档宽度样张 / 键位 / 键路由 / 模态互斥 / View 集成 / **端到端：实机 update JSON → handleSessionUpdate → 过滤只剩 /model**）
  - **M6（2026-09-18）**：鼠标点击选行已实装 —— 点某行 = 选中并执行（无参数命令直接跑；带参数命令先补全），行号映射与渲染共用 `palShown` 口径（`-cmdtest` ⑤b 断言）。本地命令（如 /panel）不与引擎混排（v1 只展示引擎广告的命令）
- **现状（M4d 实装，2026-09-18）**：`/resume`（不带参数）由客户端接管 —— 引擎只认 `/resume <session_id>`，这里改成弹出**会话选择列表**（新文件 `ui_sess.go`）：
  - `session/list`（letcode 单页返回、不发行游标；列表按引擎工作区自动过滤）：`{"sessions":[{"sessionId","cwd","title"?,"updatedAt"?}]}`
  - `session/load` `{sessionId, cwd, mcpServers:[]}`：引擎**先把历史重放**成 `session/update` 通知（只有文本：`user_message_chunk` / `agent_message_chunk`；工具卡不进重放），再回应答 `{modes, configOptions}` 并重播命令表
  - 列表形态：标题行「载入会话 · N 条」+ 最多 6 行 + 「… 还有 N 条」；选中行 `❯` 强调绿、时间 `MM-DD HH:MM` 与 id 暗灰（窄屏先丢 id 再丢时间）；↑↓ 选 / enter 载入 / esc 关闭
  - 载入流程：清空对话区 → 落一行"载入会话 …（引擎将重放历史）" → 重放逐条上屏（用户消息走新的 `user_message_chunk` 分支）→ 应答到达后置"已载入"；期间 `busy` 锁发送、状态栏显示「载入会话…」
  - **保序（2026-09-19 事故修复）**：`session/load` 的完成事件**必须走事件通道**（`loadSessionAsync` 把合成事件塞回 `c.Events`），不能当普通 tea.Msg 从 goroutine 返回 —— 读循环先把整段重放推进 `c.Events`、然后才回应答，而 UI 一个 tea 周期才消费一条事件，直接返回会让完成事件插到重放中间（`loading`/`busy` 被提前翻假 → 后面的 `agent_message_chunk` 全走 `lastAssistant` 合并路径 → 回答并进第一条助手消息、被顶到消息区上方，用户看到"只剩提问"）。合并路径另有 `!m.loading` 护栏双保险；载入中也不抢状态栏/流式光标。自检 `-loadtest` 9 项
  - 探针（无 TTY 真机）：`-sessions`（真拉列表）/ `-load <id>`（真载入 + 统计重放）；自检：`-sesstest`（11 项）
- **现状（M4e 实装，2026-09-18）：客户端命令护栏** —— 引擎侧规则（letcode `src/acp/slash.rs` / `src/command.rs` 已取证）：**带 hint 的命令不给值 = 必拒**（回 `Usage: /…`）；**本地专有命令 = 必拒**（回 `… is not available over ACP`）；**未知文本照常当提问**。据此把"引擎必拒"的三种形态在本地拦下（`ui_cmd.go` 的 `cmdGuard`），省一次白跑的往返：
  - ① 广告命令缺参数（如 `/reasoning`）→ 琥珀提示「还缺参数：off|none|minimal|low|medium|high|xhigh —— 敲 / 打开命令列表，选中回车即可补全」
  - ② 取值清单型 hint（`a|b|c` 形态）参数越界（如 `/permission plan`）→ 琥珀提示「没有 plan 这个取值；可选：safe|default|auto|yolo」
  - ③ 本地专有命令（23 条：`/help /? /exit /quit /perm /language /lang /agents /think /thoughts /tools /tool-output /scrollbar /panel /theme /fake /tree /context /mcp /skill /child /children /parent`；出处 = letcode `src/command.rs` 32 条减 ACP 广告的 9 条）→ 琥珀提示「本地 TUI 专有，ACP 模式下引擎不接受」；**只对"引擎没广告过"的名字生效**（将来引擎放行即自动让位）
  - 拦下时**不发引擎、不清输入框、不进历史**，消息区落一条 `kWarn`（琥珀 `!` 前缀）；`/resume` 仍走 M4d 的会话列表（优先于护栏）
  - 引擎真拒绝时（如 `/reasoning high` 被模型/供应商拒）→ ACP 错误格式化成人话（`参数不合法（-32602）：…` / `请求被拒（-32600）：…`）+ 尾随"怎么办"提示行
  - 连按回车不刷屏：紧挨着的上一条同款琥珀提示不重复落行（`Feed.Last()` 去重；`-cmdtest` 有断言）
  - 自检：`-cmdtest` 25 项（新增：护栏三态拦截 / 合法命令与普通文本放行 / `submit` 走护栏不发引擎、输入保留 / 护栏去重）

## 8. 压缩（Compact）

- 触发：用户 `/compact` ✅ ｜ 引擎自发 ⚠️（ACP 无主动通知）
- **现状（M5d/M9/M11 实装，2026-09-18 → 09-19）**：协议取证（driver.rs / projection.rs）——`/compact` 成功 = 正常回合结束；失败回 JSON-RPC 错误（`letcode could not compact the session context[: …]`）；**压缩过程引擎不发任何进度通知**（Compaction 事件不进 projection）→ 进行中由客户端模拟。
  - 进行中（M11 起在**工作区**，输入栏正上方）：只有**进度条 + 百分比**——不放状态词、不放乱码（用户点名）；进度条**从左往右填充**（填充段 = 主题渐变 A→B→C 逐格取色，未填充 = 暗格 `░`，右端 `42%`）；进度是模拟值（协议无进度）：`p(t)=96·t/(t+5s)` 时间渐近、封顶 96%，由 120ms 快心跳推进（`stepCompactProg`）
  - 完成：`usage_update` 的 used 下降 ≥5 个百分点 → 进度跳 100%（满条 + 绿色百分比）+ 上下文条从旧值缓动到新值（心跳 1/4 步长）；回合结束时工作区收成一行收据 `✓ 上下文已压缩：87.3k → 12.4k`（绿色）；引擎自发整理走消息区 kSys 行（措辞加注「（引擎自动整理）」）
  - 收尾三态：未见下降 → 「压缩请求已完成（未观察到用量下降）」（琥珀）；取消 → 「压缩已取消 · 5.2s」；引擎报错 → 「压缩失败」（红色；错误块另给"怎么办"）
  - 消息区不再出现压缩条目（M9 的 `kCompact` 条目退役）——压缩的戏整个归工作区
  - 自检：`-compacttest` 10 项（识别/进度模拟单调封顶/工作区进度条左起填充+百分比+无文字乱码/填充语义/心跳推进与完成跳 100%/usage 下降缓动与收据/噪声不误报/自发措辞/收尾四态/指纹）
- 透明度原则：进度条只用前景色（填充取主题渐变、未填充取暗色 `ruleStyle`）；mono 主题自动退化为灰阶
- 💤 上游：压缩状态也走 ACP 通知（扩展点 #2）

## 9. 子代理

- v1（协议内）：
  - 已实装（M5d，2026-09-18）——协议形状全部实机取证（`@explorer` 真机 trace + catalog.rs / driver.rs / result.rs）：
  - 父级工具卡（agent**explore / agent**oracle / agent\_\_fixer …）按 §1.4 渲染（"派遣"类图标）
  - 结果 JSON 解析 `structured_result` → 自算 chips：`read 6 · commands 12 · checks 2` ✅
  - 子代理的权限/问题请求 → §2 + `explorer ›` 来源徽章 ✅
  - 工具名：`agent__explore/fixer/oracle/designer/librarian/general`（+ 管理类 `agent__jobs/status/wait/cancel`）
  - in_progress 的 title 是 `agent__explore {紧凑 JSON}`（噪音）→ 弃用 title，改从 `rawInput.task` 造摘要行：`explore · <task>`
  - completed 的 `content.text` 是 JSON 信封 `{ok, tool, data:{summary, structured_result{files_read, files_changed, commands_run, validation, findings}}}` → 结果行换成 `data.summary`、chips 由计数拼出（read · changed · commands · checks · findings，只列非零）
  - 工具卡头行：齿轮换 `»` 派遣图标（颜色仍随状态）；chips 接在结果行尾
  - 子权限来源徽章：标题 `"<agent>: <summary>"` → 拆出 `fixer ›` 徽章（只认已知角色名，普通冒号标题不误伤）
  - 自检：`-agenttest` 6 项（识别/信封/chips/摘要/工具卡渲染/来源徽章/符号宽度）
- 💤 上游愿望：活体活动行（`fs__read — read … · has more`）、`/child` 跳转、完整面板（扩展点 #1）

## 10. 滚动与流式

- 自动滚底；上滚暂停跟随 + 提示 `回看历史中（下方还有 N 行）`
- 滚轮 3 行 / PgUp、PgDn 8 行 / g、G 首尾（可选）；Shift+拖拽保留系统复制
- **右缘滚动条（M4a，已实装 2026-09-18）**：消息区最右一列（外挂 1 格，不占内容宽、不触发重排）
  - 拇指 `┃`（强调绿 `#00E7A4`）/ 轨道 `│`（深灰 `#3C3C3C`）；只前景色（§0.5）
  - 几何照 crush：拇指长 = max(1, 视口高 ² / 内容高)；拇指顶 = 距顶行数 × (视口高 − 拇指长) / (内容高 − 视口高)
  - 不溢出 → 整列留白；右栏可见 → 该列常驻（右栏不因条出现/消失而横移）
  - 自检：`-scrolltest`（不溢出 / 底部贴底 / 中部悬中 / 顶部贴顶 + View 集成行数与总宽）
  - 开放项：crush 默认「滚动后 2 秒自动隐藏」，我们 v1 选**常显**；要自动隐藏挂 blink 心跳即可（一行判断）

## 11. 主题与字符安全

- 主题：`~/.config/letcode/themes/*.toml` → lipgloss 样式 + glamour 样式表（snow 首发；两个前端一套主题）
- **透明优先**：`*_bg` token 默认不落地（见 §0.5）；强调一律用前景色
- 字符纪律：widthcheck 清单（`❯ ⌛ ▍ ▌ ┃ ✓ ◐ ○` 等）；所有宽度用 lipgloss.Width

- **现状（M5c 实装，2026-09-18；M7 色板重定调）**：色板集中到 `theme.go` 的 `Theme`（20+ 前景色 token），全 UI 样式变量由 `applyTheme` 重建（渲染点零改动）；内置主题 `sprout`（默认 · 翠绿）/ `mono`（灰度）；`/theme` 客户端命令（列清单 / 切换；不发引擎、不进历史、不落盘 —— 重启回默认）；切换时作废 glamour 渲染器缓存（`mdRenderers` 构造时把颜色烤了进去）。自检：`-themetest`（7 项：注册表 / 命令全链路 / mono 全灰探测器 / 四色存在性 / 渐变几何 / 缓存作废 ×2 —— markdown 渲染器 + 消息条渲染缓存）
- **markdown 配色边界（M6 口径 · 2026-09-18 更新）**：M5c 接入行内代码（`Code` = 强调绿）与粗体（`Strong`）；**M6 再接入标题（Heading + H1-H6）、链接（Link/LinkText）与斜体（Emph）**——mono 主题下 markdown 样张已全灰（themeSample 含 markdown 段）。glamour dark 里仍随 dark 的只剩：**代码块 chroma 语法制导色**与表格/引用块边框（样张刻意不含这些；§14 留档后续）

## 11.5 主题色板 · 翠绿渐变（2026-09-17 定调；2026-09-19 重定调）

**基调**：内置默认主题 `sprout` —— 主体色为**清新的绿色渐变**（亮白绿 → 体绿 → 深林绿）。
观感目标：绿绿的、清爽、低侵略性（2026-09-19 用户定调"绿绿的"清新观感；**只用色彩语言，不使用任何 IP 字样**）。
渐变只作用于**前景色**（与 §0.5 叠加：永远不铺底色）。

**渐变锚点**（`lipgloss.Blend1D` CIELAB 混合）：

| 锚点 | 色值      | 说明                      |
| ---- | --------- | ------------------------- |
| A    | `#C8F5A0` | 亮白绿（亮端 · 新芽感）   |
| B    | `#5FCC6E` | 体绿（中段 · 主强调基调） |
| C    | `#2F7D46` | 深林绿（深端）            |

**应用清单（M5c 落地情况）**：

- Spinner / 活动指示：字符沿渐变循环（实现参考 crush `anim.Settings{GradColorA, GradColorB, CycleColors}`）—— 未做，留给 M5d 动画批
- 边框流动（border blend rotation）：参考 lipgloss `examples/blending` —— 未做（v1 无边框）
- 上下文进度条（§3）：**已实装（M5c）** —— 绿档填充格按 A → B → C 取值（`lipgloss.Blend1D`，CIELAB 混合）；≥60% 转琥珀、≥85% 转柔红（阈值仍按 §3）
- 绿条回执（§1.6）：**已实装** —— 用强调绿 `#4EE05E`（`Theme.Accent` token）
- 权限面板强调线（§2）：**保持琥珀**（警示语义 —— 与"需要操作"绑定，不并入绿色系；见 §15 ㉙）
- Logo / Header（未来加入时）：整体渐变 —— 未做

**实现时机（M5c 已落地）**：已按常量形式内置（`theme.go` 的 sprout / mono）+ `/theme` 切换命令；接 `letcode themes` 换肤体系仍留后续。

**强调绿（M7 · 2026-09-19 更新）**：

- 强调绿定为 `#4EE05E`（体绿系，清新明快；旧值 `#00E7A4` 青绿随 2026-09-19 色板重定调退役），用于**小面积强调元素**：用户消息竖条（§1.1）、行内代码（§1.2）、滚动条拇指、弹层/列表选中 ❯
- **家族关系（M5c 定论，2026-09-18；色值随 M7 更新）**：强调绿不并入渐变锚点、也不替换 B —— 它是独立 token（`Theme.Accent`），专管小面积强调；渐变 A/B/C 管"沿长度过渡"的元素（上下文条）。"成功"语义色独立为 `OK`（`#8CE28A` 柔绿），与强调绿同族不同值
- 落点（M5c 起）：`theme.go` 的 `Accent` token；渲染点 = `ui_feed.go` 的 `userBarStyle` / `scrollThumbStyle`、`md.go` 的 `Code.Color`

## 11.6 灵动感设计（参考 pi，2026-09-17 定调）

**基调**：这个 TUI 不需要太严肃——状态呈现可以有趣、有人味，但**信息必须真实**
（趣味的载体是文案与动效，不是假数据）。参考：本机 pi（pi-working-line 扩展 + extension 生态）。

**工作行（working line）**：把干巴巴的“思考中…/回复中…”升级为拼装式动态行：

```
酝酿中… · 正在读文件 · 12s · ≈2.4k tokens
```

- 短语段：随机动词短语（**中文原创短语库**；机制参考 pi-working-line 的 190 动词库）
- 工具段：当前工具动作（读文件 / 跑命令 / 搜索…）
- 时长段：回合已用时长
- 估算段：chunk 字符数 /4 粗估 token（用 ≈ 标注非官方值）
- 段可配置（开/关），默认精简：短语 + 时长
- 收尾语：结束时来一句过去式短语 + 时长（如“一气呵成 · 12.4s”），替代干巴巴的“回合结束”

**现状（M10 实装 → M11 搬位，2026-09-19 · 用户点菜）**：工作行 / 收尾行整条从状态栏搬到**输入栏正上方的工作区**（用户口径"放上面，长点"；占行时消息区让位，整屏行数守恒）：

```
[乱码]....,.... · 思考中 · 12s · ≈↑31.8k ↓412
```

- 落点：工作区（M11）——忙时（普通回合）= 工作行；忙时（压缩回合）= 进度条 + 百分比（只此两项，见 §8）；闲时 = 上一回合的收尾行；都没有则整行不占位
- 乱码块：8 字符逐帧重掷（M11 从 4 加长——用户"太短了，长点"；字符集照 crush `internal/ui/anim/anim.go` 的 availableRunes），亮度沿块左暗 → 右亮（主题渐变的亮度缩放）；**纯动效、零信息**——它的任务只是"引擎在动"
- 点：`....,....` 静态锚点（用户点名；**特意不做动画**——闪动只属于乱码块，点是静）
- 状态词：思考中 / 回复中 / 取消中 / 载入会话（沿用 §3 的语义与配色；压缩回合不走工作行——用户点名不要"压缩中"这类词）
- 时长：本回合已用时长（`turnStart` 打点，`formatElapsed`）
- token：`≈↑used ↓本回合字符数÷4`——协议只给 used/size（收发计数拿不到），**整段带 ≈ 不装精确**；缺项自动跳过（没起点就没时长段，没用量就没 token 段）
- 心跳：忙时 **120ms/帧**（`blinkInterval`，快档），闲时回落 500ms（乱码要够闪，但不能一直烧重绘）
- 收尾语：`closingLine` 四档（<3s / <15s / <60s / 更长）× 同档轮换（`turnSeq`），如「一气呵成 · 12.4s」；取消 → 「中途收兵 · 3.2s」、出错 → 「半路卡壳 · 1m 2s」
- 自检：`-worktest` 6 项（乱码块 8 格宽/逐帧重掷/同参可复现/亮度渐变 + 工作行拼装 + 缺项跳过 + 收尾语四档与轮换 + 工作区忙/闲/空三态 + 状态栏撤下工作行）
- 💤 未做：短语段（中文动词库）、工具段（当前工具动作）、段开关——状态词目前仍是固定词

**工作指示器**：帧动画可自定义（参考 pi `setWorkingIndicator({frames, intervalMs})`）——
`· → • → ● → •` 呼吸点，或与柔绿渐变联动的字符循环（§11.5）。

**状态位结构**：footer 状态位按“key → 段”注册（参考 pi `setStatus(key, text)`），
为未来信息段（git 分支 / 费用 / 用量）预留结构。

**可扩展性（轻量版）**：Go 单二进制不做 TS 级扩展系统；
以“配置驱动 + 内置趣味开关”替代（短语库可换、段可配、主题可换）。

## 12. 键位总表

| 场景     | 键                      | 行为                                      |
| -------- | ----------------------- | ----------------------------------------- |
| 输入     | enter                   | 发送                                      |
| 输入     | esc                     | 清空 / 回合中=取消回合（session/cancel）  |
| 输入     | ↑/↓                     | 历史（M4+）                               |
| 权限     | y / a / n               | 允许一次 / 始终允许 / 拒绝                |
| 权限     | esc                     | 取消（答 cancelled）                      |
| 弹层     | ↑↓ / tab / enter / esc  | 选择 / 补全 / 执行 / 关闭                 |
| 消息区   | 滚轮 / PgUp / PgDn      | 滚动                                      |
| 消息区   | tab                     | 进入工具卡选择态（↑↓ 选卡 / enter 展开）  |
| 消息区   | enter                   | 选择态 = 展开/收起选中卡；平时 = 发送     |
| 消息区   | esc（选择态）           | 退出选择态（不取消回合）                  |
| 输入     | ↑/↓（空输入或翻历史中） | 翻历史输入（有草稿时仍归消息区滚动，M4c） |
| 会话列表 | ↑↓ / enter / esc        | 选择 / 载入（session/load 重放）/ 关闭    |
| 全局     | ctrl+b                  | 右栏开关                                  |
| 全局     | ctrl+c                  | 退出                                      |
| 全局     | /                       | 命令弹层                                  |

## 13. v1 边界与愿望清单（实锤）

| #   | 内容                             | 状态                                                                            |
| --- | -------------------------------- | ------------------------------------------------------------------------------- |
| 1   | 子代理活体行 / /child / 完整面板 | 💤 需上游扩展 ACP projection                                                    |
| 2   | 压缩状态主动通知（含自发压缩）   | 💤 同上                                                                         |
| 3   | 上下文分段明细                   | 💤 协议只给 used/size 总数                                                      |
| 4   | token ↑↓ / t/s                   | ✅ token ↑↓ 已实装（M10 · 客户端估算带 ≈，见 §11.6）；t/s 未做（引擎不给速率）  |
| 5   | elicitation 表单                 | ✅ 已实装（M5a，2026-09-18）：能力声明 + `elicitation/create` 面板，见 §2.5     |
| 6   | /theme、/child、/parent 等命令   | ⚠️ /theme 已由前端自管（M5c：sprout / mono 切换）；/child、/parent 仍随协议拒绝 |

## 14. 里程碑映射

- M2：§1（消息区渲染全件套）+ §2（权限流）
- M3：§3（状态栏）+ §4（右栏）+ §5（待办）
- M4：§6（输入增强）+ §7（命令弹层）+ 会话列/载
- M5：§8 §9 §11 §11.5 §12（压缩/子代理增强 + 主题 + 柔绿渐变落地 + 键位打磨）
- M1.6（**已实装**，2026-09-17）：§1.2 回答区去前缀 + §1.3 思考三档（标题/耗时/全文）+ §1.4 工具行颜色化 + §1.7 回合分隔行
- M1.7（**已实装**，2026-09-17）：§1.1 用户消息强调绿竖条 + 鼠标点击加粗 + §1.2 行内代码换强调绿
- M2a（**已实装**，2026-09-17）：§2 权限面板（y/a/n/esc + 队列 + 模态）+ §1.6 回执（kReceipt 绿条）；M2b（工具卡展开/输出 tail）待做
- M1.8（**已实装**，2026-09-18）：§1.2 回答多段化（修复"最终回复停在画面外"）+ `-trace` 原始流量落盘（排障）
- M2b（**已实装**，2026-09-18）：§1.4 工具卡 v1（鼠标点击展开/收起 + 自动三态 + 输出 tail 末 8 行 + rawInput 展示；键盘选择态留 v2）
- M2b v2（**已实装**，2026-09-18）：§1.4 键盘选择态（Tab 进出 / ↑↓ 选卡 / enter 展开收起 / esc 只退模式；选中卡头行 `▌` 强调绿；`-navtest` 状态机自检 9 条全绿）
- M3 状态栏（**已实装**，2026-09-18）：§3 实况数据（模式徽章 / 上下文条阈值色 / 模型名·思考档 / 窄屏降级）
- M3b 右栏（**已实装**，2026-09-18）：§4 会话信息/上下文/待办小列表 + §5 待办列表 + ctrl+b 开关 + 窄屏自动隐藏
- M4a 右缘滚动条（**已实装**，2026-09-18）：§10 竖向滚动条（1 格外挂列 + crush 几何；`-scrolltest` 几何与 View 集成自检）；顺带修正 handleClick 的右栏命中边界（计入滚动条列）
- M4b 命令弹层（**已实装**，2026-09-18）：§7 数据接 available_commands_update（9 条引擎命令）+ 过滤 / ↑↓ / tab 补全 / enter 执行（带参数先补全）/ esc 关闭 + 窄屏降级；`-cmdtest` 22 项全绿
- M4c 输入历史（**已实装**，2026-09-18）：§6 ↑↓ 召回发送过的内容（空输入/翻历史中才接管，有草稿仍滚消息区）；`-histtest` 12 项全绿
- M4d 会话列/载（**已实装**，2026-09-18）：§7 `/resume` → 会话选择列表（session/list）+ session/load 重放上屏（新增 user_message_chunk 分支）；`-sesstest` 11 项全绿；真机探针 `-sessions` / `-load <id>` 均通过。**2026-09-19 保序修复**：完成事件改走事件通道（与重放同一条 FIFO）+ 合并路径 loading 护栏；`-loadtest` 9 项全绿（详见 §15 ㊶/㊷）
- M4e 命令护栏 + 错误块宽度修正（**已实装**，2026-09-18）：§1.5 宽度预算修正（gutter 2 格）+ `Detail` 提示行 + `clipLine` 兜底；§7 客户端护栏（缺参数 / 参数越界 / 本地专有命令 → 琥珀提示，不发引擎）；`-feedtest` 宽度断言 + `-scrolltest` 右块锚定不变量（新增错误/护栏/工具卡混合样张）；同批打磨：`wrapText` 词边界优先折行（英文/命令名不再被截断）+ 护栏提示去重；`-cmdtest` 25 项与全部自检回归全绿
- M3c 输入队列（**已实装**，2026-09-18）：§3 状态栏「排队 N」+ §6 回合锁改队列（忙时提交 → `kQueued` 暗色条目 → 回合结束原地转正 + 自动开下一回合，不产生重复消息）；`-queuetest` 6 项全绿
- M5a 追问表单 elicitation（**已实装**，2026-09-18）：§2.5 —— `initialize` 广告 `elicitation.form`，接 `elicitation/create` 并渲染表单（单选/多选/1..9 直选/enter 提交/esc 拒绝，accept·decline·cancel 三种应答）；`-elicittest` 7 项全绿；真机探针 `-smokeelicit` 跑通（引擎 `question` 工具从被婉拒 → `2 fields (completed)`）
- M5b Todos 钉面板（**已实装**，2026-09-18；2026-09-19 搬家至消息区底部）：§5 消息区底部 `# Todos · 计数` 固定面板（不滚动；占几行消息区扣几行）+ `/todos` 客户端开关（不发引擎）+ `plan` 快照驱动刷新；`-todostest` 5 项全绿
- M5c 主题系统（**已实装**，2026-09-18）：§11 / §11.5 —— 色板集中（`theme.go` 的 `Theme` + `applyTheme`）+ 内置 `sprout` / `mono` + `/theme` 客户端命令 + 上下文条柔绿渐变（A→B→C，CIELAB 混合）；`-themetest` 7 项全绿（含 mono 全灰探测器 + 两类缓存作废）
- M5d 压缩动画 + 子代理增强（**已实装**，2026-09-18）：§8 —— `/compact` 瞬态（「正在整理上下文」+ 跳跃小条）+ usage 下降缓动/回执 + 收尾与错误指纹（`-compacttest` 7 项）；§9 —— agent 信封解析（summary + chips）、`»` 派遣图标、子权限来源徽章（`-agenttest` 6 项）
- M6 打磨批（**已实装**，2026-09-18）:① 压缩条渐变+扫描光（替换跳跃小条，`-compacttest` 渐变断言）；② 应用内复制（右键/ctrl+y；`-copytest` 4 项）；③ 工具卡聚合折叠 + edit diff 特例（`-feedtest` 扩展断言）；④ markdown 标题/链接/斜体主题化 + 弹层鼠标点选（`-themetest`/`-mdtest`/`-cmdtest` 扩展断言）—— 全量 19 套自检绿
- M7 修复与翠绿重定调（**已实装**，2026-09-19）：§2.5 追问面板 View 漏拼装修复（syncLayout 扣高度但 View 未上屏 → 面板隐形 + esc 被吃成 decline；`-elicittest` 补 View 集成断言）+ §11.5 色板重定调（sprout 翠绿：Accent/OK/Warn/Reply/Heading/渐变换新值，Err/ErrSoft 柔红不变；只用色彩语言不用 IP 字样）—— 全量 19 套自检绿
- M9 压缩可视化搬家（**已实装**，2026-09-19）：§8 —— 压缩的"戏"从底部状态栏搬进消息区：`kCompact` 条目（标题 + 满宽渐变扫描光条"彗星"）+ 动画期 120ms 快心跳 + 完成原地转收据（余晖 12 帧后收敛一行）+ 收尾三态（无下降 / 取消 / 失败）；状态栏只留静态状态词（`-compacttest` 重写为 9 项）—— 全量 18 套自检绿
- M10 灵动工作行（**已实装**，2026-09-19）：§11.6 §3 —— 忙时状态栏左段 = 乱码块（照 crush 逐帧重掷 + 亮度渐变，120ms 快心跳）+ 静态点「....,....」+ 状态词 + 时长 + ≈↑↓ token；闲时 = 收尾语（四档 + 轮换，替代「回合结束」），取消/出错各有措辞；`-worktest` 5 项 —— 全量 19 套自检绿
- M11 工作区与压缩进度条（**已实装**，2026-09-19 · 用户点菜）：§0 §3 §8 §11.6 —— ① 工作行/收尾行从底部状态栏搬到**输入栏上方的工作区**（全宽、不再被状态栏挤短），压缩时工作区只显示**进度条 + 百分比**（无状态词、无乱码）；② 进度条**从左往右填充**（填充 = 主题渐变、未填充 = `░`、右端百分比），进度 = 时间渐近模拟（封顶 96%，`usage` 下降跳 100%）；③ 压缩不再进消息区（M9 的 `kCompact` 条目退役），回合结束工作区收成一行收据；④ 状态栏左段只留轻量静态词 —— `-compacttest` 10 项 + `-worktest` 6 项，全量 19 套自检绿

## 15. 决策记录（Decision Log）

- 2026-09-17 ① 思考块 = **混合**（流式实时显示 → 结束自动折叠为 `Thought · 1.2s`，enter 展开）
- 2026-09-17 ② 权限呈现 = **混合式**（底部面板操作 + 消息区占位/绿条回执）
- 2026-09-17 ③ 工具卡展开 = **键盘 v1**（enter；鼠标点击 M5）+ 运行中自动展开/完成自动折叠
- 2026-09-17 ④ 右栏开关 = **ctrl+b**（+ `/panel` 兜底 + 窄屏自动隐藏）
- 2026-09-17 ⑤ 视觉基调 = **终端透明优先、禁止大块底色**（§0.5）
- 2026-09-17 ⑥ 主体色定调 = **柔绿渐变**（内置默认主题 `sprout`，色板与落点见 §11.5）——应用于 spinner / 边框 / 进度条 / 强调元素
- 2026-09-17 ⑦ 灵动感定调 = **不严肃、有人味、信息真实**（参考本机 pi 的 working-line 与 extension 生态；详见 §11.6）
- 2026-09-17 ⑧ 回答区去前缀 = **退役铅笔 ✎**，整块 2 格缩进、无符号（轮次区隔交给回合分隔行；§1.2 / §1.7）
- 2026-09-17 ⑨ 工具行状态 = **颜色表达**（成功柔绿 `#79C77D` / 失败柔红 `#C97B7B` / 进行中灰；成功不再输出"完成"二字），且**完成时工具身份必须显示完整**；错误色同步降饱和（errStyle `#FF6B6B` → `#D98A8A`）（§1.4）
- 2026-09-17 ⑩ 思考块 = **对齐 /thoughts 三档的 Full 档**：按周期分块，每块 `~ 标题` + 全文 + 块尾耗时行 `· 2s`（§1.3）
- 2026-09-17 ⑪ 回合分隔行 = **`◇ 模型 via 供应商 in 总耗时` + 右侧补满横线，整行无彩色**（照 crush 观感；§1.7）
- 2026-09-17 ⑫ 用户消息形态 = **左侧强调绿竖条 `#00E7A4`**（细线 `│`；鼠标点击后换半块 `▌` 加粗，照 crush 的 blurred/focused 两态），消息区 ❯ 前缀退役（§1.1）
- 2026-09-17 ⑬ 行内代码配色 = **粉红改强调绿** `#00E7A4`（glamour dark 原为 256 色 203；§1.2）
- 2026-09-17 ⑭ 权限流 = **面板模态 + y/a/n/esc 一键操作**（输入区上方、按 options 实际 kind 拼提示、队列一次一条、esc 先 cancel 回合再回 cancelled）；应答 id 必须**原样回传**（字符串 UUID，§2 踩坑实录）
- 2026-09-18 ⑮ 回答分段 = **与思考周期同款边界**（非正文事件 = 段落边界；每段独立条目、就地呈现）。事故背景：单槽位实现下，"工具调用后的最终回复"会被并进第一段的位置（画面外），观感=模型没回；取证方式=引擎会话转录（`~/.config/letcode/sessions/*.jsonl`）+ 新增的 `-trace` 原始流量落盘双对照；加固（同日）：endAssistantSegment/endThoughtCycle 留 lastAssistant/lastThought 锚点，迟到的正文/思考 chunk 并回上一段（应对"回合应答先到"的通道时序竞争）
- 2026-09-18 ⑯ 工具卡 v1 = **鼠标点击 + 自动三态**（运行展开 / 完成折叠 / 失败展开；手点后不再自动）；展开区 = `$` 命令行（或紧凑 JSON）+ 输出 tail 末 8 行（`│` 竖条 + 截断标记）；▸/▾ 指示跟在齿轮后。键盘选择态留 v2（enter 与发送冲突）
- 2026-09-18 ⑰ 键盘选择态 = **显式模式（Tab 进出）**：enter 与"发送"的冲突不靠重映射解决——Tab 进入后 enter 归选择态（展开/收起），打字/点击/esc/tab 退出即恢复发送；esc 在模式内只退模式（防误触取消回合）。候选卡只算"有内容的卡"；选中卡头行换 `▌` 且**颜色跟随齿轮状态色**（成功绿 / 失败红 / 进行中灰），一眼看出选中的卡是成是败（宽度不变）；状态栏联动「选择工具卡 i/n」+ 键位提示
- 2026-09-18 ⑱ "实际失败"（ToolBad）= 引擎 failed ∪ 命令级失败（shell 类摘要 `exit ≠ 0`）；红齿轮 / 红选中条 / 默认展开三处统一跟随。背景：用户实测 `shell__exec ls -la`（exit 1）显示为绿色——引擎语义如此（工具跑完了，exit code 是数据）；原版 letcode 无此推导，由前端补一刀（用户要求）
- 2026-09-18 ⑲ 状态栏（M3）= 左「引擎状态 · 模式徽章 · 上下文条 · 模型」+ 右快捷键；阈值色 <60 绿 / <85 黄 / ≥85 红；窄屏从尾部丢段（模型 → 条 → 徽章）；模态（权限/选择态）优先。数据源与 ACP schema 字段名对齐（currentModeId / used / size / configOptions.id = mode|reasoning_effort；camelCase）
- 2026-09-18 ⑳ 右栏（M3b）= 消息区右侧 28 格信息栏 +「 │ 」分隔列；ctrl+b 开关、<100 列自动隐藏；数据接 session_info_update.title 与 plan.entries（全量快照）；点击右栏不落消息区
- 2026-09-18 ㉑ 滚动条（M4a）= **外挂 1 格列、不占内容宽**（不触发重排）：溢出即常显（不做 crush 的 2 秒自动隐藏——v1 求状态可预测）；拇指=强调绿、轨道=深灰；不溢出整列留白；几何照 crush、数据复用现有 allLines/normH/scroll（零新增状态）；右栏可见时该列常驻，右栏不横移
- 2026-09-18 ㉒ 命令弹层（M4b）= **引擎广告即弹层数据**（available_commands_update，命令以 prompt 文本执行，引擎侧分派；未知文本仍当提问）。定稿：`/` 且无空格才弹；esc 只关不删；enter **无参数直接执行 / 带参数只补全**（口令与发送键冲突的正面解法）；弹层行数从消息区扣出、与权限面板同一套"钉子位"机制；↑↓/tab 在弹层开着时优先于滚动与工具卡选择态（模态序：权限 > 弹层 > 选择态 > 滚动）
- 2026-09-18 ㉓ 输入历史（M4c）= **↑↓ 分档接管**：空输入（或翻历史中）翻历史，有草稿时 ↑↓ 仍是滚消息区（保住既有手感，滚轮/PgUp/PgDn 不受影响）；历史仅内存、连续重复去重、↓↓ 还原草稿、手改即退出浏览
- 2026-09-18 ㉔ 会话列/载（M4d）= **`/resume` 客户端接管**：引擎只认带 id 的 `/resume`，所以不带参数时弹会话列表（session/list 单页、按工作区过滤），enter 走 `session/load` —— 引擎**先重放历史再应答**（重放只有文本，工具卡不入内），客户端先清屏再逐条上屏（新增 `user_message_chunk` 分支）；载入期间 busy 锁 + 状态栏「载入会话…」。模态序更新：权限 > **会话列表** > 弹层 > 选择态 > 滚动
- 2026-09-18 ㉕ 命令护栏（M4e）= **把"引擎必拒"的话本地说清**：引擎规则（带 hint 缺参数必拒 / 本地专有命令必拒 / 未知文本当提问）在客户端镜像成三种拦截；关键词是"省一次白跑 + 输入不丢"（拦下时输入框内容保留，用户接着补参数）。同批修掉错误块的**宽度预算**（前缀实测宽）并补 `Detail` "怎么办"提示行——起因：用户截图里 `/resume` 的引擎报错把"右栏顶出去了"（错误行超预算 2 格）
- 2026-09-18 ㉖ 回合锁改**输入队列**（M3c）= 用户口径"AI 在动我又发了一条，这条应该排队，等它执行完再插进去"：忙时提交不拒绝、不报错，而是入队（消息区以 `kQueued` 暗色条目呈现「· 排队中」），当前回合一结束就**原地转正**（同一条目换回 kUser）并自动开下一回合——状态栏「排队 N」告知还压着几条。选择原地转正而非"重新发一条"，正是为了不重蹈早前截图里"用户消息出现两次"的观感
- 2026-09-18 ㉗ 追问走**协议级 elicitation**（M5a）= Grok 分类里"追问是协议新能力、不是 M5 主题"——认同，所以单独立一个里程碑提前做：`initialize` 广告 `elicitation.form` + 接 `elicitation/create` + 表单面板（选项式作答，非自由文本）；关键是**三种应答语义要对**：accept（带 content）/ decline（用户不想答，面板 esc）/ cancel（客户端取消、不支持的 mode）。真机取证：不广告时引擎自动婉拒（`question 4 questions · the ACP client does not support form elicitation`），广告后同一个 question 工具走完整表单并 `completed`
- 2026-09-18 ㉘ Todos 钉面板（M5b）= **消息区顶部固定面板 + `/todos` 开关**：面板不参与滚动（读待办不该翻历史），占几行就从消息区高度里扣几行（整屏行数守恒，与权限面板同属"钉子位"机制）；`/todos` 归客户端（引擎命令表里没有它，不拦会被当成普通提问发给模型）；最多 6 条 + `… 还有 N 条`；数据沿用 `plan` 全量快照。布局实现要点：`syncLayout` 先定宽度再算钉面板行数（行数只由待办条数决定，与宽度无关）
- 2026-09-18 ㉙ 主题系统（M5c）= **色板集中 + 样式变量重建**：颜色不散落在渲染点，全部收进 `theme.go` 的 `Theme`；`applyTheme` 重建包级样式变量（渲染点无感），并作废 glamour 渲染器缓存（它是构造时烤色的）。内置 `sprout`（默认 · 柔绿渐变）+ `mono`（灰度）；`/theme` 客户端命令切换（引擎命令表没有它；不落盘，重启回默认）。柔绿渐变第一处落点 = 上下文条填充格 —— 用官方 `lipgloss.Blend1D`（CIELAB 混合），正中 §11.5 "避免中间发灰"的诉求。`mono` 兼作自检的硬编码探测器：切灰后样张里若还有彩色 SGR，说明有渲染点没走主题。缓存纪律：换主题必须作废两类"烤色"缓存 —— glamour 渲染器（mdRenderers 重建）与消息条渲染缓存（itemLines 按 themeEpoch 代次校验，不 bump ver 也重渲染）
- 2026-09-18 ㉚ 用户消息回显去重 = **引擎会回发，客户端要消费**：实机 trace 证明正常回合开始引擎也发同文本 `user_message_chunk`（早先以为只有 session/load 重放才发），叠加客户端本地回显 → 同一条消息在消息区显示两次（用户截图实况）。修法：`localEcho` 登记最近一次本地回显文本（submit / 出队转正两处登记），`handleSessionUpdate` 里同文本且在 loading 之外即消费掉；迟到回发（回合结束后才到）也覆盖——登记不提前清。自检 `-echotest` 5 项（含"重放照收""文本不匹配照收"两个反例）
- 2026-09-18 ㉛ 压缩可见性（M5d）= **协议无进度 → 客户端模拟 + usage 下降当完成信号**：`/compact` 是命令回合（成功 = end_turn、失败 = 明确错误、过程零通知；driver.rs / projection.rs 取证）。所以：提交即置瞬态（状态栏「正在整理上下文」+ 8 格跳跃小条）；完成靠 `usage_update` 下降 ≥5 个百分点识别（上下文条缓动 + 回执；引擎自发整理走同口径、措辞加注）；取消不算完成；`could not compact` 有本地提示。5 个百分点阈值防普通波动误报
- 2026-09-18 ㉝ M6 打磨批（用户点菜，四项）= ① **压缩条升级**：8 格「渐变扫描条」（Blend1D 主题渐变底 + 高光带扫动，`scaleBright` 亮度缩放；仍 8 格 / 无底色 / mono 全灰）；② **应用内复制**（§1.8 新增节）；③ **聚合折叠 + diff 特例**（§1.4：连续完成卡 → `read 3 · checks 1 · expand` 一行、点击逐张吐出、选择态禁用；`edit__apply_patch` 红绿 diff 双栏/单栏）；④ **markdown 全量主题化**（标题/链接/斜体入主题，§11）+ **弹层鼠标点选**（§7）—— 全量 19 套自检绿
- 2026-09-18 ㉜ 子代理可见性（M5d）= **信封解析替代标题噪音**：`agent__*` 工具的 in_progress title 是「工具名 + 参数 JSON」（不可读），完成 content 是 `{ok, data:{summary, structured_result}}` 信封（实机 trace 取证）→ 摘要行取 `rawInput.task`、结果行取 `data.summary`、chips 数 `structured_result` 各数组；头行 `»` 派遣图标；子权限标题 `"<agent>: …"` 拆来源徽章（只认已知角色名）。上游愿望（活体活动行 / `/child` 跳转）仍留扩展点
- 2026-09-19 ㉞ 追问面板隐形 bug（真机截图取证）= **syncLayout 与 View 两关脱节**：M5a 落地时 syncLayout 把面板行数从消息区扣出，但 View() 的拼装序列漏了 renderElicitPanel —— 布局"留了位"、画面没有它、键盘却被面板接管；用户按 esc（想脱身）被吃成 decline，引擎收到"用户拒绝回答"（截图：工具卡裸奔 questions JSON + 已拒绝回答回执）。修法 = View 补 ②.2 拼装 + `-elicittest` 新增 View 集成断言（整屏行数守恒 + 面板可见 + 宽度/背景合规）。教训入册：**新增钉面板必须同时过 syncLayout 与 View 两关**（权限/弹层/会话列表/待办四件套都是这么活的，追问面板漏了第二关）
- 2026-09-19 ㉟ 色板重定调 = **翠绿（只求"绿绿的"，不用任何 IP 字样）**：用户点名 IP 风格后收敛为纯色彩语言 —— Accent `#00E7A4`→`#4EE05E`、OK `#79C77D`→`#8CE28A`、Warn `#FFD166`→`#F5A25C`（暖橙）、Reply/Link→`#7CC8F8`（天空蓝）、Heading→`#EFF7EA`、渐变 A/B/C→`#C8F5A0`/`#5FCC6E`/`#2F7D46`；**Err/ErrSoft 柔红保持不变**（失败语义的既有肌肉记忆）。所有硬编码色断言同步（themetest/mdtest/feedtest/statustest/cmdtest/sesstest 六处 RGB 抽查）
- 2026-09-19 ㊱ 压缩可视化搬家（M9 · 用户点菜）= **"戏"从状态栏搬进消息区 + 只做诚实的不确定态动效**：用户口径"不喜欢 compact 的东西放下面，应该放消息那里，然后一个炫酷的进度条"。实现：① `beginTurn(/compact)` 落一条 `kCompact` 条目（`◐ 正在整理上下文 · 12s` + 满宽扫描光条），跑完**原地**转收据（不额外落系统行——同 M3c"原地转正"的同一考虑）；② 扫描光 = 主题渐变压暗底 + 彗星（头亮尾暗、三角波往返；`compactFeedBar(step, w, fade)`），fade 用于完成后 12 帧余晖渐隐、随后收敛成一行；③ **心跳提速**：条目在动时心跳 500ms → 120ms（`blinkInterval()`），动画结束自动回落——这是"顺滑"的关键（普通心跳下扫描光一顿一顿）；④ 引擎无进度协议 → **不造百分比**（只做不确定态动效 + 真实耗时），延续 §11.6"趣味不靠假数据"；⑤ 状态栏撤下扫描条（只留静态状态词）。自检证据：`-compacttest` 9 项，含 60 格亮度谱样张（`[##%%%%::::…]` → `[…---===+++***#…]`）直观证明彗星在移动
- 2026-09-19 ㊲ 灵动工作行（M10 · 用户点菜）= **乱码是戏、数字是真的**：用户口径"crush 那样的乱码在闪，后面跟一串不闪的点；闪的节奏像 pi；右侧要有 thinking、时间、↑↓ token"。落点 = 状态栏左段（原「思考中…」位）。① 乱码块 4 字符逐帧重掷（字符集照 crush `anim.go` 的 availableRunes），亮度沿块左暗 → 右亮（`scaleBright`）；② 点 `....,....` **静态**（特意不动画——闪动只属于乱码块）；③ 状态词 / 时长真实（状态词沿用思考中/回复中/正在整理上下文/取消中）；④ token 估算 `≈↑used ↓本回合字符数÷4`（协议只给 used/size，收发计数拿不到——宁标 ≈ 不装精确）；⑤ 心跳忙时 120ms（乱码要够闪）、闲时回落 500ms；⑥ 收尾语 `closingLine` 四档 + 同档轮换替代「回合结束」，取消 → 「中途收兵」、出错 → 「半路卡壳」。教训：状态栏左段是"引擎状态"位，忙时归工作行——降级链（尾部丢段）保证工作行恒在，排队段次之（queuetest 窄预算断言随之从 16 格改 40 格）
- 2026-09-19 ㊳ 工作区 + 压缩进度条（M11 · 用户点菜 · 三点反馈）= **"放上面、长点、别来回扫"**：① 工作行/收尾行从底部状态栏搬到输入栏正上方（新"工作区"，占一行、消息区让位），宽度与输入块对齐（全宽，"被挤短"不再）；② 压缩可视化也搬进工作区，且进行中**只剩进度条 + 百分比**（用户点名不要"回复中/压缩中"状态词、不要乱码）；③ 进度条改为**从左往右填充**（否掉"一道光来回扫、到中间左边又暗"的观感），填充段 = 主题渐变逐格取色；④ 进度模拟（协议无进度）：`p(t)=96·t/(t+5s)` 时间渐近封顶 96%（不确定态、不装精确），观察到 usage 下降跳 100%、上下文条缓动 + 回合结束收据（`✓ 上下文已压缩：87.3k → 12.4k`）；⑤ 消息区 `kCompact` 条目退役（压缩的戏整个归工作区）；⑥ 状态栏左段只留轻量静态词（空闲/载入/引擎退出）——动态内容不再两处重复
- 2026-09-19 ㊴ 取消不卡死（真机事故修复）= **协议层"取消"= 通知 + 应答两件事，只做一半就是卡死**：用户连续拒绝/取消审批后，界面钉死"取消中"51s、排队消息发不出（截图取证）。根因：`cancelPerms`（权限面板 esc）发了 `session/cancel`、改了 UI 占位，却**漏了对引擎 `session/request_permission` 反向请求的 cancelled 应答**（`settlePermCancelled` 只 SetText）——引擎的权限请求永远挂着 → 工具调用不返回 → 回合永不结束 → `session/prompt` 永不应答 → busy 永不释放。修法：① `cancelPerms` 逐条回 `{"outcome":{"outcome":"cancelled"}}`（当前请求 + 队列；nil client 护栏供自检）；② **取消看门狗**兜底：`cancelAt` 记发起取消时刻，心跳里超过 `cancelGrace`（15s）引擎还不结束回合就 `forceCancelFinish` 强制本地收尾——先 `turnGen+1` 作废在途结果，引擎迟到的旧回合结果按 gen 过期丢弃（不会误杀新回合；gen=0 的自检自构消息不参与校验，旧测试零改动）；esc 取消路径同样落 `cancelAt`；③ 新增 `-canceltest` 8 项（应答体形状 / cancelPerms 状态机 / gen 守卫 / 强制收尾）。正常取消引擎秒回，看门狗只是引擎失常时的保险丝
- 2026-09-19 ㊵ Todos 钉面板搬家（M5b · 用户点菜）= **消息区顶部 → 底部（照 Claude Code）**：用户口径"todolist 是在顶部，我想移动到下面来，像 ClaudeCode 一样"。实现只动拼装：`View()` 里 pinned 行从 left 块头部改到尾部（消息行在前、钉面板行在后），滚动条列索引随之改为只跟消息行对齐（`i < len(bar)`，钉面板行不画条）；高度机制原样（`syncLayout` 的 todosH 照扣、整屏行数守恒）；右栏与命令弹层命中测试不受影响（left 块总高不变）。顺带修：弹层点击命中测试补算 elicitation 面板高度（此前漏算——追问面板开着时点弹层会整段偏移）。自检 `-todostest` ④ 断言改为"钉面板在消息区底部"（标题行索引 = 顶部留白 1 行 + feed 高）
- 2026-09-19 ㊶ /resume 只剩提问（真机事故修复）= **"应答"与"重放"在两条通道上赛跑，完成事件被当普通 tea.Msg 返回就会插队**：用户截图——/resume 回去只剩三连问，回答全不见了（其实都有回答）。根因链：`loadSessionAsync` 在 goroutine 里等 `session/load` 应答并直接 `return sessLoadDoneMsg{...}`；而读循环是**先把整段历史重放一条条推进 `c.Events`、然后才把应答交给等的 Call()**，UI 却是一个 tea 周期才消费一条事件（每条还要重新 arm `waitEvent`）——完成事件于是插到重放中间，`finishLoad` 把 `loading`/`busy` 提前翻假，后续 `agent_message_chunk` 全部走 `lastAssistant` 合并路径（那是给"应答先到、正文 chunk 后到"设计的），回答并进**第一条**助手消息、被顶到消息区上方看不见。修法两件：① 完成事件改走事件通道——`loadSessionAsync` 把合成事件（`methodLoadDone`）塞回 `c.Events`，与重放同一条 FIFO，天然排在最后（`ACPEvent` 加 `Err` 字段承载错误，`Update` 的 `acpEventMsg` 分支分流到 `finishLoad`）；② 合并路径加 `!m.loading` 护栏 + 载入中不抢状态栏/流式光标（双保险，防将来再出现时序漂移）。自检 `-loadtest` 9 项（重放保序 / 条目顺序 / 完成落最后 / 收尾状态 / 两道护栏 / 失败路径 / 重放照收）。**教训入册：跨 goroutine 的"完成信号"与流式事件必须走同一条有序通道**（同源问题：回合应答 `turnDoneMsg` 也走独立通道，所以才有 lastAssistant 那套补丁——载入场景不该复用那套）
- 2026-09-19 ㊷ 引擎侧重放边界（已知局限，待用户定夺）= **`session/load` 的重放来自引擎的"活跃协议帧"（模型上下文），不是会话全史**：`engine.rs` 用 `restored_messages_from_protocol_frames(active_protocol_frames())` 构造应答前的重放，`runtime_context.rs` 的 `active_protocol_frames()` 只取 `visibility == Active` 的帧——**压缩（`history_applied`）退休掉的旧历史不进重放**，且 `driver.rs` 的 `replay_history` 还会跳过 `ConversationRole::Summary`、只发文本（工具卡不入内）。实测（`-load <id>` 探针）：纯聊天会话完整重放（user=3/agent=3）；压缩过的长会话几乎空（user=1/agent=0、甚至 0/0）。要不要让 letcode 改成按全史重放（并补工具卡）属于引擎侧改动，按项目纪律需用户批准后再动

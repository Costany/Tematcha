# letcode-tui

[letcode](https://github.com/letr007/letcode) 的 Bubble Tea 前端。通过 ACP（`letcode acp`）驱动引擎，界面自绘。

```
go run .
```

引擎路径和工作区目前写在 `main.go` 的常量里。无 TTY 自检：`-widthcheck`、`-feedtest`、`-permtest`、`-navtest`、`-statustest`、`-paneltest`、`-scrolltest`、`-cmdtest`。
